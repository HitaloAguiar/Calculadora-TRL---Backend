quarkus.hibernate-orm.database.generation=drop-and-create

# Configure your datasource
# quarkus.datasource.db-kind = postgresql
# quarkus.datasource.username = calculadora_trl
# quarkus.datasource.password = 123456
# quarkus.datasource.jdbc.url = jdbc:postgresql://localhost:5432/calculadora_trl

# Configuracao do CORS
# quarkus.http.cors.enabled=true
# quarkus.http.cors.origins=http://localhost:4200
# quarkus.http.cors.methods=GET,POST
# quarkus.http.cors.access-control-max-age=24H

# src/main/resources/application.properties (valores locais / dev)
quarkus.http.host=0.0.0.0
quarkus.http.port=${PORT:8080}

# Exemplo local (substituído por env vars no Render)
quarkus.datasource.jdbc.url=${QUARKUS_DATASOURCE_JDBC_URL:}
quarkus.datasource.username=${QUARKUS_DATASOURCE_USERNAME:}
quarkus.datasource.password=${QUARKUS_DATASOURCE_PASSWORD:}
quarkus.datasource.db-kind=postgresql

# Ative CORS e deixe origin vazia por padrão
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=${QUARKUS_HTTP_CORS_ORIGINS:*}
quarkus.http.cors.methods=GET,POST
# quarkus.http.cors.headers=Content-Type,Authorization
